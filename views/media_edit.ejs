<h1>Редактировать медиа</h1>
<form method="POST" action="/media/<%= media.id %>/edit">
    <input type="text" name="title" value="<%= media.title %>" required><br>

    <select name="type" required>
        <% types.forEach(t => { %>
            <option value="<%= t %>" <%= t === media.type ? 'selected' : '' %>><%= t.toUpperCase() %></option>
        <% }) %>
    </select><br>

    <input type="number" name="year" value="<%= media.year %>"><br>
    <input type="number" name="price" value="<%= media.price %>" required><br>
    <input type="number" name="total_copies" value="<%= media.total_copies %>" required><br>
    <input type="number" name="available_copies" value="<%= media.available_copies %>" required><br>
    <button type="submit">Сохранить</button>
</form>
